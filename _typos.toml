[files]
# The following files will be excluded from spell check during commits
extend-exclude = [
    "packages/paddlefleet_ops/third_party",
]

[default]
# Ignore 1-3 letter words, refer to https://github.com/crate-ci/typos/issues/1079
extend-ignore-words-re = ["^[a-zA-Z]{1,3}$"]
# refer to https://github.com/crate-ci/typos/blob/master/docs/reference.md#example-configurations
extend-ignore-re = [
    # Ignore lines by `# typos: disable-line`
    "(?Rm)^.*(#|//)\\s*typos:\\s*disable-line$",
    # Ignore block by `# typos: off` and `# typos: on`
    "(?s)(#|//)\\s*typos:\\s*off.*?\\n\\s*(#|//)\\s*typos:\\s*on"
]

[default.extend-words]
# FleetCore specific words
arange = "arange"
astroid = 'astroid'
cacl = 'cacl'
CANN = 'CANN'
Clas = 'Clas'
clen = 'clen'
datas = 'datas'
dota = 'dota'
dout = "dout"
entrys = 'entrys'
feeded = 'feeded'
grad = "grad"
Halfs = 'Halfs'
kinf = 'kinf'
# Part of the HuggingFace dataset name "DoclingMatix" used by CI fixtures
Matix = 'Matix'
operants = 'operants'
Optin = 'Optin'
padd = 'padd'
pash = 'pash'
setp = 'setp'
splited = 'splited'
splitted = 'splitted'
statis = 'statis'
UNEXPECT = 'UNEXPECT'
tood = 'tood'
unpacket = "unpacket"
vaccum = 'vaccum'
